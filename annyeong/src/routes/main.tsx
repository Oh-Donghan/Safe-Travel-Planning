import axios from 'axios';
import { useParams } from 'react-router-dom';

export default function Main() {
  // const params = useParams();

  // console.log(params.id);
  
  // axios.get(
  //   `https://maps.googleapis.com/maps/api/geocode/json?address=${encodeURI(params.id)}&key=${
  //     import.meta.env.VITE_G_MAP_API_KEY
  //   }`
  // ).then(response => {
  //   console.log(response);
  // })
  // .catch (err => {
  //   console.log(err);
  // })

  return <div>main</div>;
}
